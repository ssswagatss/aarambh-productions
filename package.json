{
  "name": "aarambh-weddings",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "cp index.html public/index.html && cp main.js public/main.js && cp style.css public/style.css || mkdir public && cp index.html public/index.html && cp main.js public/main.js && cp style.css public/style.css",
    "start": "vercel dev"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "resend": "^4.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
