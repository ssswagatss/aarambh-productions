{
  "name": "aarambh-weddings",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html main.js style.css public/",
    "start": "vercel dev"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "resend": "^4.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
